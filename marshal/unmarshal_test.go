package marshal_test

import (
	// TODO: Add back when tests are written
	// . "github.com/mexisme/multiconfig/marshal"

	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("multiconfig/marshal", func() {
	It("Placeholder", func() {
		Skip("No tests")
		Expect(true).To(BeTrue())
	})
})
